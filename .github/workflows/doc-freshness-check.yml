name: Doc Freshness Check

on:
  schedule:
    - cron: '0 2 * * 1' # Weekly Monday 02:00 UTC
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check stale / unreviewed docs and open issues
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Cap issue creation per run to avoid a "first run" flood.
          # Set MAX_ISSUES=0 for unlimited.
          MAX_ISSUES: 50
          GRACE_DAYS: 7
        run: bash .github/scripts/check_freshness.sh
